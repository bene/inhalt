---
import type { With } from "@inhalt/astro";

export interface Props {
  text: With<
    string,
    {
      label: "Hallo";
    }
  >;
}

const { text } = Astro.props;
---

<p class="font-bold">1{text}</p>
